{
  "name": "inter-claude-channels",
  "version": "0.1.0",
  "description": "A peer-to-peer channel that lets Claude Code sessions talk to each other.",
  "license": "MIT",
  "type": "module",
  "bin": "./server.ts",
  "scripts": {
    "start": "bun install --no-summary && bun server.ts",
    "test": "bun test",
    "demo": "bun scripts/demo.ts",
    "db:generate": "drizzle-kit generate",
    "doctor": "bash scripts/doctor.sh",
    "setup": "bash scripts/install.sh",
    "uninstall": "bash scripts/uninstall.sh"
  },
  "keywords": [
    "claude",
    "claude-code",
    "channel",
    "mcp",
    "ipc",
    "agents"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.10",
    "drizzle-kit": "^0.31.10"
  }
}
